spring:
    application:
        name: search-service
    datasource:
        url: jdbc:postgresql://localhost:5432/plantrek_search
        username: user
        password: password
    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

inventory-service:
    baseUrl: http://localhost:8000/inventory-service